export declare class GcpStorage {
    private bucketName;
    private proxyConfig?;
    private storageAPI;
    constructor(bucketName: string, proxyConfig?: {}, storageAPI?: string);
    getFiles(opts: {
        prefix: string;
    }): Promise<any>;
    static create(bucketName: string, proxyConfig?: {}): GcpStorage;
}
