import type { CommandModule } from 'yargs';
export declare class LocalVersionCmd implements CommandModule {
    aliases: string[];
    desc: boolean;
    command: string[];
    handler(): Promise<void>;
}
export declare const localVersionCmd: LocalVersionCmd;
