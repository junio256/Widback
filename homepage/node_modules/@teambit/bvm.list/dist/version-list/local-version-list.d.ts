import { LocalVersion } from '../version';
import { VersionList } from './version-list';
export declare class LocalVersionList extends VersionList {
    entries: LocalVersion[];
    constructor(entries: LocalVersion[]);
    sortBySemver(order?: 'asc' | 'desc'): LocalVersionList;
}
