import { RemoteVersion } from '../version';
export declare class GcpVersion {
    private fileName;
    private host;
    private md5hash;
    private timeCreated;
    private metadata;
    private protocol;
    constructor(fileName: string, host: string, md5hash: string, timeCreated: string, metadata: {
        [key: string]: string;
    }, protocol?: string);
    get version(): string;
    get url(): string;
    /** is version is stable version  */
    get stable(): boolean;
    toRemoteVersion(): RemoteVersion;
    toObject(): {
        url: string;
        version: string;
    };
}
