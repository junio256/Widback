import { GcpStorage } from '@teambit/gcp.storage';
import { RemoteVersionList } from '../version-list';
export declare class GcpList {
    private gcpStorage;
    private osType;
    private arch;
    private releaseType;
    constructor(gcpStorage: GcpStorage, osType?: string, arch?: string, releaseType?: string);
    list(): Promise<RemoteVersionList>;
    rawFiles(): Promise<any>;
    static create(releaseType?: string, osType?: string, arch?: string, proxyConfig?: {}): GcpList;
}
