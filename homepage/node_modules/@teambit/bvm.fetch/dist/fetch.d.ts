export declare type FetchOpts = {
    override?: boolean;
    overrideDir?: boolean;
    destination: string;
};
export declare type FetchResults = {
    resolvedVersion: string;
    downloadedFile?: string;
};
export declare function fetch(version: string, opts: FetchOpts): Promise<FetchResults>;
